# 🌱 Crop Expert

**Crop Expert** is a Python-based deep learning project that detects potato leaf diseases using a Convolutional Neural Network (CNN).

## 📌 Features

- 🧠 CNN model with **92% accuracy**
- 🖼️ Image-based leaf disease classification
- 📊 Achieved **10% boost** in accuracy over baseline models
- ⚡ Accelerated detection by **45%**
- 🧪 Built and tested in **Jupyter Notebook**

## 🛠️ Tech Stack

- Python
- TensorFlow / Keras
- Jupyter Notebook
- NumPy, Pandas, Matplotlib
- OpenCV (for image preprocessing)

## Running It

```bash
pip install tensorflow numpy pandas matplotlib opencv-python jupyter
jupyter notebook crop_expert.ipynb
```

Run the notebook's cells in order — it loads a potato leaf image dataset, trains/evaluates the CNN, and reports classification accuracy on held-out images.

> **Note:** `crop_expert.ipynb` in this repo is currently an empty placeholder file (no cells committed). The description above reflects the project as previously described in this README; the training/inference code itself needs to be restored or re-added to the notebook before the commands above will do anything.

## Deployed Link

Not applicable — this is a notebook-based model, not a hosted web app.
